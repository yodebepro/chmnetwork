Upload index.html and all published_*.json files to the same website folder.
After changing videos, music, radio, or TV live mode in Admin, export/replace the matching JSON file.
Small uploads can be embedded into JSON; large uploads should be hosted as links.
